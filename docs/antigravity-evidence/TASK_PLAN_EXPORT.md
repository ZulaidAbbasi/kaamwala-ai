# Task Plan Export — KaamWala AI

> Per-phase task breakdown with acceptance criteria, generated by Antigravity.

---

## Phase-by-Phase Task Breakdown

### Phase 2: Gemini NLU Integration

**Endpoint:** `POST /parseRequest`

**Tasks:**
1. Create `functions/src/services/gemini/geminiClient.ts`
2. Implement `parseServiceRequest(rawText)` with Gemini 2.0 Flash
3. Build keyword-based fallback for API failures
4. Create `functions/src/endpoints/parseRequest.ts`
5. Create `src/screens/ServiceRequestScreen.tsx`
6. Wire into Express routes

**Acceptance Criteria:**
- ✅ Roman Urdu input parsed correctly
- ✅ Fallback produces valid result when API unavailable
- ✅ `npm run build` exits 0

---

### Phase 5: Provider Ranking

**Endpoint:** `POST /rankProviders`

**Tasks:**
1. Create `functions/src/services/rankingEngine.ts`
2. Implement 12-factor scoring (distance, rating, relevance, etc.)
3. Add Gemini explanation (explains but does NOT choose)
4. Create `functions/src/endpoints/rankProviders.ts`
5. Create `src/screens/ProviderRankingScreen.tsx`

**Acceptance Criteria:**
- ✅ Deterministic scoring (same input = same output)
- ✅ Registered providers get booking eligibility bonus
- ✅ Gemini explanation does not override scores
- ✅ `npm run build` exits 0

---

### Phase 10: Fallback & Recovery

**Endpoints:** `POST /simulateProviderCancellation`, `/resolveDispute`, `/handleNoProviderFound`, `/handleLowConfidenceRequest`

**Tasks:**
1. Create 4 endpoint handlers
2. Implement 6 scenario types
3. State-before → reasoning → recovery options → state-after
4. Create `src/screens/FallbackRecoveryScreen.tsx`

**Acceptance Criteria:**
- ✅ Each scenario produces structured recovery
- ✅ Apology messages generated in English + Roman Urdu
- ✅ Firestore records saved
- ✅ SIMULATION BOUNDARY labels visible
- ✅ `npm run build` exits 0

---

### Phase 12: Outcome Evaluation

**Endpoint:** `POST /evaluateOutcome`

**Tasks:**
1. Create `functions/src/agents/outcomeEvaluatorAgent.ts`
2. Implement 12 performance metrics
3. Implement 9-dimension before/after comparison
4. Grade assignment (A+ to F)
5. Create `src/screens/OutcomeEvaluationScreen.tsx`

**Acceptance Criteria:**
- ✅ Score 0–100 with grade
- ✅ Before/after cards for 9 dimensions
- ✅ Baseline comparison toggle
- ✅ Firestore saved
- ✅ `npm run build` exits 0

---

## Task Pattern

Every phase followed this Antigravity-assisted pattern:

```
1. Define endpoint (input/output/rules)
2. Generate backend service
3. Generate endpoint handler
4. Wire into Express
5. Build and verify: npm run build → exit 0
6. Generate mobile screen
7. Typecheck: npm run typecheck → exit 0
8. Update VERIFICATION_LOG.md
```

---

## 📸 Screenshot Placeholder

> **ACTION REQUIRED:** Add screenshot of Antigravity conversation showing a task plan being defined.

```
[ ] Screenshot: Task definition with input/output/rules
[ ] Screenshot: Acceptance criteria discussion
```
